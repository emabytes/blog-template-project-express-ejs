<%-include('./partials/headNav')%>

<section id="all-articles">
    <% data.forEach(elt => { %>
    <article <%= elt.id %>>
        <a href="#"><img src="images<%= elt.url %>" alt=""></img></a>
        <h3><a href=""><%= elt.title %></a></h3>
    </article>
    <% }) %>
</section>

<%-include('./partials/footer')%>
</body>

</html>